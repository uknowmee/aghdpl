{
    "triggerTaskOnSave.tasks": {
        "Create preview": [
            "md/*.md", // <-- Glob pattern to specify which files should trigger the task
        ],
    },
    "makefile.configureOnOpen": false
}